<h1>{{ 'checkout.title'|trans({'%boutique%': 'Demo Shop'}) }}</h1>

{{ form(form) }}

{% include 'sumary.html.twig' %}

<a href="{{ path('checkout_confirm') }}">
    {{ 'checkout.titel'|trans({'%shop%': 'Demo Shop'}) }}
</a>
